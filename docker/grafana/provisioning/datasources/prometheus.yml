# Auto-registers Prometheus as Grafana's datasource on container start
# — no manual "Add data source" click-through needed. Milestone 7.
apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
