# Production runtime dependencies. Identical to base — FastAPI/Kafka
# client dependencies landed directly in base.txt (Milestones 4/6) since
# every environment, not just prod, needs them.
-r base.txt
