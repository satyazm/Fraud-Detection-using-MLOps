# Development/local tooling on top of the base runtime dependencies.
-r base.txt

ruff==0.6.9
black==24.10.0
mypy==1.13.0
types-PyYAML==6.0.12.20240917
pandas-stubs==2.2.3.241009
pytest==8.3.3
pytest-cov==5.0.0
pre-commit==3.8.0

# Milestone 6: fastapi.testclient.TestClient requires httpx.
httpx==0.28.1

# Milestone 8: dashboard/app.py, a local-only live demo dashboard —
# not used by any deployed image (Dockerfile.api/.worker don't install
# dev.txt), verified conflict-free via `pip check`.
streamlit==1.60.0
