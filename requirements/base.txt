# Core runtime dependencies shared by every environment.

pyyaml==6.0.2
python-json-logger==2.0.7

# Phase 2: data ingestion, validation, preprocessing, splitting.
pandas==2.2.3
# Bumped from 1.26.4: feast requires numpy>=2.0.0,<3 and
# apache-beam (Milestone 5's apache-flink pulls it in) requires
# numpy>=1.14.3,<2.2.0 — 2.1.3 is the version actually shared by both
# constraints. Verified: pandas/sklearn/xgboost/lightgbm/matplotlib and
# the full test suite all still pass against it.
numpy==2.1.3
scikit-learn==1.5.2
matplotlib==3.9.2
# Bumped from 17.0.0: apache-beam requires pyarrow<17.0.0; feast/mlflow/
# apache-flink are all satisfied by 16.1.0 too.
pyarrow==16.1.0

# Milestone 3: model training, comparison, MLflow tracking/registry.
xgboost==2.1.1
lightgbm==4.5.0
mlflow==2.17.2

# Milestone 4: Kafka streaming.
confluent-kafka==2.15.0

# Milestone 5: real-time feature platform (Feast + Redis + PyFlink).
# apache-flink needs `setuptools<81` present (for apache-beam's
# pkg_resources-based build) *and* `pip install --no-build-isolation`
# — see `make install`/`install-dev` and README. JAVA_HOME must also
# point at a JDK 11/17/21 (e.g. `brew install openjdk@17`); PyFlink
# embeds a local Flink mini-cluster via a JVM gateway.
feast[redis]==0.65.0
apache-flink==2.3.0

# Milestone 6: FastAPI real-time inference service. Already present
# transitively (feast bundles its own FastAPI/uvicorn-based feature
# server), pinned explicitly here since Milestone 6 depends on them
# directly, not just through feast.
fastapi==0.140.0
uvicorn==0.34.0

# Milestone 7: observability (Prometheus metrics, Evidently AI drift
# reports). prometheus_client was already present transitively (same
# reason fastapi/uvicorn were); evidently is a new, heavy dependency
# (pulls in litestar, plotly, statsmodels, nltk, ...) but installed
# clean — verified with `pip check` against every existing pin
# (numpy/pandas/pyarrow/sklearn/xgboost/lightgbm/mlflow), no conflicts.
prometheus_client==0.24.1
evidently==0.7.21
